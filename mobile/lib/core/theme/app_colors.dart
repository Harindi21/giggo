import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- Brand ---
  static const Color primary = Color(0xFF121D4A); // deep navy
  static const Color primaryDark = Color(0xFF0A1544);
  static const Color primaryLight = Color(0xFF2A3766);

  static const Color accent = Color(0xFFFF851C); // orange CTA
  static const Color accentDark = Color(0xFFE06A00);

  // --- Surfaces & backgrounds ---
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceBlue = Color(0xFFD2E5FF); // info cards
  static const Color background = Color(0xFFF7F7F7);
  static const Color navBar = Color(0xFF313030); // bottom nav

  // --- Text ---
  static const Color textPrimary = Color(0xFF121D4A);
  static const Color textBody = Color(0xFF191919);
  static const Color textMuted = Color(0xFF6B7280);
  static const Color textOnDark = Color(0xFFFFFFFF);

  // --- Borders & dividers ---
  static const Color border = Color(0xFFD8D8D8);
  static const Color divider = Color(0xFFF4F2F3);

  // --- Status
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFDC2626);
  static const Color info = Color(0xFF2563EB);
}
